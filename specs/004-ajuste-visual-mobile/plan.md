# Plan - 004 Ajuste Visual y Movil

## Fase 1. Sistema visual compartido

- Unificar tokens de color, sombras y estados de foco en `styles.css`.
- Extender los ajustes moviles a `.premium-glass` y helpers compartidos.

## Fase 2. Layout responsivo por pantalla

- Mejorar encabezados, stepper, grids y navegacion del formulario en `index.html`.
- Reordenar filtros, metricas y listas del tablero en `dashboard.html`.
- Ajustar nomina, panel lateral, modal e invitaciones en `invitacion.html`.

## Fase 3. Markup dinamico alineado

- Adaptar `renderStudents()` y la pantalla de confirmacion de `app.js` al mismo sistema visual y a distribucion movil.

## Fase 4. Verificacion minima

- Ejecutar `npm run verify` y `npm run verify:smoke`.
- Dejar nota del recorrido manual recomendado para confirmacion visual final.
